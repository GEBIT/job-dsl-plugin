<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>plugin</artifactId>
    <version>4.65</version>
    <relativePath />
  </parent>
  <artifactId>job-dsl-parent</artifactId>
  <version>1.84</version>
  <packaging>pom</packaging>
  <name>Job DSL Parent</name>
  <url>https://github.com/jenkinsci/job-dsl-plugin</url>
  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>
  <modules>
    <module>job-dsl-ast</module>
    <module>job-dsl-core</module>
    <module>job-dsl-plugin</module>
  </modules>
  <scm child.scm.connection.inherit.append.path="false" child.scm.developerConnection.inherit.append.path="false" child.scm.url.inherit.append.path="false">
    <connection>scm:git:https://github.com/${gitHubRepo}.git</connection>
    <developerConnection>scm:git:git@github.com:${gitHubRepo}.git</developerConnection>
    <tag>job-dsl-1.84</tag>
    <url>https://github.com/${gitHubRepo}</url>
  </scm>
  <properties>
    <revision>1.84</revision>
    <changelist>-SNAPSHOT</changelist>
    <gitHubRepo>jenkinsci/job-dsl-plugin</gitHubRepo>
    <jenkins.version>2.387.1</jenkins.version>
    <!-- TODO fix violations -->
    <spotbugs.threshold>High</spotbugs.threshold>
    <spotless.check.skip>false</spotless.check.skip>
    <!-- GEBIT: versions to be used for gebit-build support -->
    <version.tiles>2.8-gebit38</version.tiles>
    <version.gebit-build-extension>1.2.2</version.gebit-build-extension>
    <version.gebit-forked>1.0.2</version.gebit-forked>
    <project.build-name>job-dsl</project.build-name>
    <tiles.applyBefore>org.jenkins-ci.plugins:job-dsl-parent</tiles.applyBefore>
    <tiles.mergeTarget>${tiles.applyBefore}</tiles.mergeTarget>
    <version.maven-enforcer-plugin>3.3.0</version.maven-enforcer-plugin>
    <version.maven-surefire-plugin>3.1.0</version.maven-surefire-plugin>
    <!-- GEBIT: end -->
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>cglib</groupId>
        <artifactId>cglib-nodep</artifactId>
        <version>3.3.0</version>
      </dependency>
      <dependency>
        <groupId>org.objenesis</groupId>
        <artifactId>objenesis</artifactId>
        <version>3.3</version>
      </dependency>
      <dependency>
        <groupId>org.spockframework</groupId>
        <artifactId>spock-core</artifactId>
        <version>1.3-groovy-2.4</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <repositories>
    <repository>
      <id>repo.jenkins-ci.org</id>
      <url>https://repo.jenkins-ci.org/public/</url>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>repo.jenkins-ci.org</id>
      <url>https://repo.jenkins-ci.org/public/</url>
    </pluginRepository>
  </pluginRepositories>
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-antrun-plugin</artifactId>
          <dependencies>
            <dependency>
              <groupId>org.codenarc</groupId>
              <artifactId>CodeNarc</artifactId>
              <version>1.6.1</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <!-- JUnit 5 does not work with the ancient version of Spock we are forced to use -->
          <dependencies>
            <dependency>
              <groupId>org.apache.maven.surefire</groupId>
              <artifactId>surefire-junit47</artifactId>
              <version>${maven-surefire-plugin.version}</version>
            </dependency>
          </dependencies>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- GEBIT: Tiles -->
      <plugin>
        <groupId>io.repaint.maven</groupId>
        <artifactId>tiles-maven-plugin</artifactId>
        <version>${version.tiles}</version>
        <extensions>true</extensions>
        <configuration>
          <applyBefore>${tiles.applyBefore}</applyBefore>
          <mergeTarget>${tiles.mergeTarget}</mergeTarget>
          <tiles>
            <tile>de.gebit.build.forked.tile:jenkins-plugin:${version.gebit-forked}</tile>
            <tile>!de.gebit.build.tile:enforce-maven:3.5.3</tile>
            <tile>!de.gebit.build.tile:enforce-no-integrations-on-release:3.5.3</tile>
            <tile>!de.gebit.build.tile:enforce-no-snapshots-on-release:3.5.3</tile>
          </tiles>
        </configuration>
      </plugin>
      <!-- GEBIT: end -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-release-plugin</artifactId>
        <configuration>
          <!-- Keep the tag names aligned with previous releases -->
          <tagNameFormat>job-dsl-@{project.version}</tagNameFormat>
        </configuration>
      </plugin>
    </plugins>
    <!-- GEBIT: Extensions -->
    <extensions>
      <extension>
        <groupId>de.gebit.build.maven</groupId>
        <artifactId>gebit-build-extension</artifactId>
        <version>${version.gebit-build-extension}</version>
      </extension>
    </extensions>
    <!-- GEBIT: end -->
  </build>
</project>
